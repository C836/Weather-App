import styled from "styled-components";

export const Input_Wrapper = styled.section`
  width: 600px;
  max-width: 90%;

  position: absolute;
  top: 50%; left: 50%;
  translate: -50% -50%;

  display: flex;
  flex-direction: column;
  row-gap: 16px;
`